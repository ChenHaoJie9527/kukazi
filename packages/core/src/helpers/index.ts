import { dirname, join, extname, basename } from "path"
import { readFile, writeFile, readdir } from "fs/promises"


export {
    readFile,
    writeFile,
    readdir,
    join,
    extname,
    basename,
    dirname
}

